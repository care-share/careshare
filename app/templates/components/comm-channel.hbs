<button class="btn btn-primary" type="button">
 <span class="glyphicon glyphicon-comment"></span> Annotations <span class="badge">{{unreadCount}}</span>
</button>
{{input action="createMessage" aria-controls="existing-user-table" placeholder="AnnotationsTEST"}}
{{#each chatMessages as |message|}}
<<<<<<< HEAD
    <div class="">
      <div class="">
         {{#if (or message.isMe (not message.src_user_id))}}
            You
         {{else}}
            {{message.src_user_id}}
         {{/if}}
        <span class="annotation-timestamp">
            {{message.parsedTimestamp}}
        </span>
      </div>
      <div class="annotation-content">
        {{message.content}}
        {{!Delete should only be available to user who creates it}}
        {{#if message.isMe}}
        <button {{action 'destroyMessage' message target='view'}} style="color:red; float: right" class="glyphicon glyphicon-remove nobackground">
        </button>
        {{/if}}
      </div>
    </div>
{{/each}}
