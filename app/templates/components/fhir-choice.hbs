<select {{action 'setChoice' patient on='change'}} class="selectpicker">
    {{#each finalChoices as |choice|}}
        <option value={{choice}}>
            <div>
                {{choice}}
            </div>
        </option>
    {{/each}}
</select>

