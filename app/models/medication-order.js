import model from 'ember-fhir-adapter/models/medication-order';
//import diffProp from 'careshare/properties/diff-property';

export default model.extend({
    // dosageInstruction is extended from medication-order-dosage-instruction-component, which has its own diffProp
});
